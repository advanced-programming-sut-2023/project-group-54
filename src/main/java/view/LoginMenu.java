package view;
import controller.*;

import java.util.Scanner;
import java.util.regex.Matcher;

public class LoginMenu {
    public String run() {
        String command;
        Matcher matcher;
        while (true) {
            command = Controller.getScanner().nextLine();
            //if command is createUser
            //if command is pickQuestion
            //if command is loginUser
            //if command is forgetPassword
            //if command is logout
        }
    }

    private void createUser(Matcher matcher) {

    }

    private void pickQuestion(Matcher matcher) {

    }

    private void loginUser(Matcher matcher) {

    }

    private void forgetPassword(Matcher matcher) {

    }

    private void logout() {

    }
}
