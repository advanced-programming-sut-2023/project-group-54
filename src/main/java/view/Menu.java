package view;

import java.util.regex.Matcher;

public class Menu {
    public static void run() {
        String command;
        Matcher matcher;
        while (true) {
            command = MainMenu.getScanner().nextLine();
        }
    }
}
